/**
 * Phase 2 placeholder ONLY. This component exists to prove the Vite + React +
 * TypeScript toolchain builds and runs (Rule 20). It intentionally contains no
 * dashboard, investigation, AI search/summary, or auth UI (Rule 3 / Rule 12) —
 * that begins in Phase 22+ (M5), per docs/MODULE_OWNERSHIP.md.
 */
function App() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "2rem" }}>
      <h1>Project SRT</h1>
      <p>Frontend development environment ready (Phase 2 scaffold).</p>
      <p>No dashboard features are implemented yet — see docs/DEVELOPMENT_SETUP.md.</p>
    </main>
  );
}

export default App;
